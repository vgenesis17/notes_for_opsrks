RUN apt-get update && \
    apt-get install -y apache2

RUN sed -i 's/<VirtualHost *:80>/<VirtualHost *:8084>/' /etc/apache2/000-default.conf && \
sed -i 's/LISTEN 80/ LISTEN 8084/' /etc/apache2/ports.conf

EXPOSE 8084


CMD ["apache2ctl","-D","FORGROUND"]
                                     