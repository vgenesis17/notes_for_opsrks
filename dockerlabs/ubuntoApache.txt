FROM ubunto:2404

RUN apt-get update && \
    apt-get install -y apache2


RUN sed -i 's/LISTEN 80/LISTEN /' /etc/apache2/ports.conf && \
    sed -i 's/<VirtualHost *:80>/<VirtualHost *:8084>/' /etc/apache2/sites-available/000-default.conf

EXPOSE 8084

CMD ["apache2ctl", "-D", "FOREGROUND"]