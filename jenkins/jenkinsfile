pipeline {
    agent {
        label {
            label 'master'
            customWorkspace "${JENKINS_HOME}/${BUILD_NUMBER}/"
        }
    }
    environment {
        Go111MODULE='on'
    }
    stages ('Test') {
        steps {
           git  'https://github.com/kodekloudhub/go-webapp-sample.git'
           sh 'go test ./...'
           }
        }
}